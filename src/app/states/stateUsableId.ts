import { atom } from "recoil";

export const stateUsableWorkId = atom({
    key: "stateUsableWorkId",
    default: 1})

export const stateUsableTaskId = atom({
    key: "stateUsableItaskId",
    default: 1})

export const stateUsableMemberId = atom({
    key: "stateUsableMemberId",
    default: 1})